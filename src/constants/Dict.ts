import type { OptionMap } from '@/framework/types'

/**
 * 用户角色映射
 */
export const MAP_USER_ROLE: OptionMap = {
  user: { value: 'user', label: '普通用户', type: 'primary' },
  admin: { value: 'admin', label: '管理员', type: 'success' },
}


